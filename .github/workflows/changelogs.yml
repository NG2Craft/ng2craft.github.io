# name: Fetch Releases from Repo
# run-name: ${{ github.actor }} is updating releases.json

# on:
#     schedule:
#         - cron: "30 5 * * 1"
#     workflow_dispatch:

# jobs:
#     changelog-schedule:
#         runs-on: ubuntu-latest

#         steps:
#             - name: Checkout repository
#               uses: actions/checkout@v4

#             - name: Fetch releases from GitHub API
#               if: github.event.schedule == '30 5 * * 1'
#               run: |
#                   mkdir -p docs/data
#                   curl -s -L \
#                     -H "Accept: application/vnd.github+json" \
#                     https://api.github.com/repos/NG2Craft/visible-armor-slots/releases \
#                     > docs/data/releases.json

#             - name: Commit changes
#               if: github.event.schedule == '30 5 * * 1'
#               run: |
#                   git config user.name "github-actions"
#                   git config user.email "actions@github.com"
#                   git add docs/data/releases.json
#                   git commit -m "Update releases.json" || echo "No changes to commit"
#                   git push
